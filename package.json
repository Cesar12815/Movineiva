{
  "name": "movineiva-global-monolith",
  "version": "1.0.5",
  "description": "Root package for MoviNeiva Global Deployment",
  "scripts": {
    "build": "cd movineva-frontend && npm install && npm run build && cd ../movineva-backend && npm install && npx prisma generate",
    "start": "cd movineva-backend && npx prisma migrate deploy && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
